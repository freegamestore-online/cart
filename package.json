{
  "name": "cart",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "scripts": {
    "dev": "pnpm --filter @cart/web dev",
    "build": "pnpm --filter @cart/web build",
    "preview": "pnpm --filter @cart/web preview",
    "typecheck": "pnpm --filter @cart/web exec tsc -b"
  }
}
